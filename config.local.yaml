hosts:
  - composer.local

plugins:
   - query-monitor

themes:
   - twentyfifteen

extensions:
   - Tester

disabled_extensions:
   - chassis/mailhog

synced_folders:
  ../init: /chassis/content/plugins/this-plugin
